meta {
  name: Create Test User
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/dev/create-test-user
  body: json
  auth: none
}

body:json {
  {
    "email": "{{testEmail}}",
    "password": "{{testPassword}}",
    "name": "{{testName}}"
  }
}
